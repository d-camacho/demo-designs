---


- prefixes:
    - prefix: "{{ region_prefix }}"
      status__name: "Active"
      type: "container"
      "!ref": "region_prefix"

    - prefix: "{{ country_prefix }}"
      status__name: "Active"
      type: "container"
      "!ref": "country_prefix"
