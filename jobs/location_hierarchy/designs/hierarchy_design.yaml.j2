---

  

locations:
  - name: "{{ region_name }}"
    location_type__name: "{{ region_type }}"
    status__name: "Active"
    prefixes:
      - prefix: "{{ region_prefix }}"
        type: "container"
        status__name: "Active"
   