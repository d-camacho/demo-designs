---

  

locations:
  - name: "{{ region_name }}"
    status__name: "Staging"
    location_type__name: "{{ region_type.name }}"
    prefixes:
      - prefix: "{{ region_prefix }}"
        status__name: "Reserved"
  

